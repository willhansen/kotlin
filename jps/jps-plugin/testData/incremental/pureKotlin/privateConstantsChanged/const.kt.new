package test

const konst CONST = "foo"

class Klass {
    companion object {
        private konst CHANGED = "new"
        const public konst UNCHANGED = 100
    }
}

object Obj : Any() {
    private konst CHANGED = "new:Obj"
    public konst UNCHANGED = 200
}
