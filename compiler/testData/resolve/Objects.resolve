package toplevelObjectDeclarations

open class Foo(y : Int) {
  ~foo()~open fun foo() : Int = 1
}

object ~A~A : Foo(0) {
  ~x~konst x : Int = 2

  fun test() {
    return `x`x + `foo()`foo()
  }
}

object B : A {}

~ns.x~konst x = `A`A.`foo()`foo()

fun f() {
  ~y~konst y = object : Foo(`ns.x`x) {
    init {
      `ns.x`x + 12
    }

    ~y.foo()~override fun foo() : Int = 1
  }

  konst z = `y`y.`y.foo()`foo()
}
