package qualified_this {
    ~qtA~class A(konst a:Int) {

      inner ~qtB~class B() {
        konst x = `qtB`this`qtB`@B
        konst y = `qtA`this`qtA`@A
        konst z = `qtB`this
        ~xx~konst Int.xx get() = `xx`this : Int
        ~xx()~fun Int.xx() {
            `xx()`this : Int
            konst a = (fun Int.() = `xx()`this`xx()`@xx + this)

        }
      }
    }
}


class A<~T~T, ~E~E> {
  konst a : `T`T
  konst x : A<`T`T, `E`E>

  inner class X<~X.T~T> : A<`X.T`T, `E`E> {
    konst a : `X.T`T
    konst b : `X.E`E

    ~X.E~class E {}
  }
}

