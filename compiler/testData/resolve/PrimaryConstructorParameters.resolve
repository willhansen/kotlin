
class C(~x~x : Int, ~y~konst y : Int) : Base(`x`x /*parameter*/), Base1 by Base1(`x`x) {
  var zx = `x`x // parameter
    get() = `!`x // inaccessible

  var zy = `y`y // parameter
    get() = `y`y // property

  init {
    konst wx = `x`x
    konst wy = `y`y
  }

  konst fx = `x`x
  konst fy = `y`y

  fun test() {
    konst ux = `!`x // inaccessible
    konst uy = `y`y // property
  }
}

class D(~a~a: Int) {
  konst a = `a`a
}