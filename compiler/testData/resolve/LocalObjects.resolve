package localObjects {
  object ~A~A {
    ~x~konst x : Int
  }

  open class Foo {
    ~foo()~fun foo() : Int
  }

  fun test() {
    `A`A.`x`x
    konst b = object : Foo {
    }
    b.`foo()`foo()

    object ~B~B {
      ~B.foo()~fun foo() {}
    }
    `B`B.`B.foo()`foo()
  }

  konst bb = `!`B.foo()
}
