fun test() {
    when (konst) {}
    when (konst x1) {}
    when (konst x2 = ) {}
    when (konst x3: ) {}
    when (konst x = 1; 42)

    when (fun foo() {}) {}
    when (class C {}) {}
    when (interface I {}) {}
    when (object Obj {}) {}
    when (typealias TA = T) {}
}
